# Environment configuration for Afritec Bridge LMS

# Flask environment: development, testing, or production
FLASK_ENV=development

# Security keys - CHANGE THESE IN PRODUCTION!
SECRET_KEY=your_secure_secret_key_here
JWT_SECRET_KEY=your_secure_jwt_secret_key_here

# Database configuration
# For development (SQLite) - leave empty to use default path in backend/instance/
# SQLALCHEMY_DATABASE_URI=

# For production (PostgreSQL) - uncomment and set when deploying:
# DATABASE_URL=postgresql://username:password@host:port/database

# JWT token expiration
JWT_ACCESS_TOKEN_EXPIRES_HOURS=1
JWT_REFRESH_TOKEN_EXPIRES_DAYS=30

# Email Configuration (SMTP)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_DEFAULT_SENDER=noreply@afritecbridge.online

# CORS configuration (comma-separated list for multiple domains)
# Production origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173,https://study.afritechbridge.online

# Development origins (optional - defaults to common localhost ports if not set)
# Add your local network IPs if testing from other devices
DEV_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3005,http://localhost:5173,http://192.168.0.4:3000,http://192.168.0.4:3001,http://192.168.116.116:3000,http://192.168.116.116:3001

# Port for the application (will be overridden by Render in production)
PORT=5000

# ============================================================
# MTN MADAPI – api.mtn.com  (primary, if CLIENT_ID is set)
# Register at https://developers.mtn.com
# ============================================================
MTN_CLIENT_ID=your-mtn-client-id
MTN_CLIENT_SECRET=your-mtn-client-secret
MTN_COUNTRY_CODE=RW

# ============================================================
# MTN MoMo Developer API – momodeveloper.mtn.com
# Used for: ValidateAccountHolderStatus, GetBasicUserinfo,
#           GetAccountBalanceInSpecificCurrency, DeliveryNotification
# Only active when MTN_CLIENT_ID is NOT set (fallback mode),
# OR both sections can be filled for full feature support
#
# To get credentials:
#   1. Register at https://momodeveloper.mtn.com/signup
#   2. Subscribe to the "Collection" product -> copy your Subscription Key
#   3. Create sandwich user + API key via curl (see backend/.env for commands)
#
# X-Target-Environment values:
#   sandbox       -> sandbox.momodeveloper.mtn.com  (testing)
#   mtnrwanda     -> production Rwanda
#   mtnuganda     -> production Uganda
#   mtnghana      -> production Ghana
# ============================================================
MTN_COLLECTION_USER_ID=your-mtn-collection-user-uuid
MTN_COLLECTION_API_KEY=your-mtn-collection-api-key
MTN_SUBSCRIPTION_KEY=your-mtn-subscription-key
MTN_API_BASE=https://sandbox.momodeveloper.mtn.com
MTN_ENV=sandbox

# Development Notes:
# - Generate SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
# - Generate JWT_SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
# - For Gmail, use App Password instead of regular password
# - Set FLASK_ENV=production for production deployment